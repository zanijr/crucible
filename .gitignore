.env
.env.local
.env.*.local
*.pem
*.key
secrets/
credentials/
node_modules/
.DS_Store
Thumbs.db

# Crucible per-project state — never commit
.crucible/

# Per-user session memory (session handoffs, local notes) — not part of the shipped plugin
.claude/memory/
